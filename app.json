{
  "name": "scrapator",
  "description": "My awesome Rails app, running on Dokku!",
  "keywords": [
    "dokku",
    "rails"
  ],
  "scripts": {
    "dokku": {
      "predeploy": "wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb && apt install ./google-chrome-stable_current_amd64.deb",
      "postdeploy": "bundle exec rake db:migrate && bundle exec rake jobs:clear && bundle exec rake scrape:start"
    }
  }
}
